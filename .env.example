# Copy this file to .env.local and fill in the blanks.
# .env.local is gitignored — never commit real values.

# Cognito user pool — same pool the mobile app uses (WEBSITE_PROJECT_CHECKLIST.md Section 3.2)
NEXT_PUBLIC_COGNITO_REGION=us-east-1
NEXT_PUBLIC_COGNITO_USER_POOL_ID=us-east-1_P3PjvFmOI

# Web-specific App Client — created in Phase 5 (Cognito Setup), do NOT reuse the
# mobile app's client ID (7pr7gvtafbodt4kmq2ckqe071u). Not yet available.
NEXT_PUBLIC_COGNITO_WEB_CLIENT_ID=

# pray4tn backend (Ruby Sinatra on Heroku)
NEXT_PUBLIC_API_BASE_URL=https://pray4tn-1909e243cd5b.herokuapp.com
